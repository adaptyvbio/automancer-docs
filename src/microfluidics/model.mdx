export const title = 'Creating a model';


# Creating a model

A _chip model_ contains all the information that describes a microfluidic chip. The most important of these is the list of inlets and oulets provided by this chip model. This model can then be used by other units to control the valves using different mechanisms, such as solenoid valves or a pump.


## Adding a diagram

```yml
diagram: diagram.svg
```
